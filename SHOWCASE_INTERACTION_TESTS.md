# Showcase Interaction Tests

## 自动浏览器回归

命令：

```bash
node scripts/test-showcase.mjs
```

结果：

```text
Showcase interaction checks: 20/20 passed
```

覆盖：

1. 12个章节DOM存在；
2. 12个章节导航存在；
3. `/demo`链接目标正确；
4. 总览模式显示12项；
5. 总览可返回当前章节；
6. hash定位第03章；
7. 流程图灯箱可打开；
8. Esc关闭灯箱；
9. 右方向键翻到第04章；
10. 正常场景闭环至已关闭；
11. 信息缺失先进入待补充；
12. 补充COLD-002后命中R009并创建待接单工单；
13. 低置信度进入待人工确认；
14. 人工确认后进入待接单；
15. P1显示即将超时；
16. P1进入超时未接单；
17. 超时改派后回到待接单并保留责任变化；
18. 验收不通过进入返修中并在原工单二次闭环；
19. 沙盒操作前后Demo SQLite工单状态与更新时间完全一致；
20. 390×844无页面级横向溢出，打印媒体隐藏交互导航。

## 人工浏览器检查

使用真实浏览器操作完成：

- 顶部章节导航；
- 底部上一页/下一页；
- 左右方向键；
- 总览与返回；
- 图片放大与Esc；
- 正常、信息缺失、低置信度、P1超时、验收返修五场景逐步操作；
- 第09章统一工单默认状态；
- 1440/1024/390产品窗口切换、缩放、全屏入口和重置；
- `/demo`跳转；
- 1440×810、1024×768和390×844视觉检查。

## 质量门禁

最终 lint、typecheck、npm test 与 production build 结果记录在 `SHOWCASE_BUILD_STATUS.md`。
