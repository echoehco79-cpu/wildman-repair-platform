# Standalone visual fix baseline

记录时间：2026-07-19

## 页面与内容保护锁

- 交付物仍为 `dist/野人先生_AI设备维修协同方案看板_Standalone.html`。
- 主章节保持 01—12，共 12 章，章节名称、顺序和业务结论不变。
- 第 09 章保持 09A 产品协同前台与 09B 数据后端两个子页。
- 产品链路保持 12 个模块；五个验证场景、八张业务表、九台模拟设备不变。
- 所有表单字段、按钮文案、状态名称、规则编号、SLA、角色、权限校验和状态机结果不变。
- 保持横向翻页、章节索引、总览、全屏、复制章节、重置、图片放大、产品沙盒、场景控制和打印功能。

## 修改前视觉与交互基线

- 总览：`screenshots/standalone-showcase/overview.png`，深炭黑全屏底色。
- 产品前台：`screenshots/standalone-showcase/standalone-09A-product.png`，内嵌系统字段字号约 6—8px。
- 页 06：To-Be 原图位于流程节点区右下角，仅为 160 × 90px 缩略图。
- 顶栏左侧包含 `OFFLINE` 标识。
- 自动回归脚本覆盖 file://、离线运行、产品交互、五场景、数据后台与打印；本轮增加 12 个顶部索引和总览索引的逐项可见性检查。

## 本轮授权的变化

- 删除顶栏 `OFFLINE` 标识。
- 修复顶部章节索引和总览索引跳转空白。
- 替换用户提供的 AS-IS 与 To-Be 流程图。
- 仅通过排版、字号、间距、溢出和布局提升可读性。
- 页 06 将 To-Be 图提升为主体证据，并通过既有节点与既有产品预览建立更清晰的视觉关系。
- 总览与第 12 章总览按钮改为暖纸白品牌视觉，不再使用大面积黑色。

## 不可变行为

- 不修改产品沙盒数据结构、路由规则、权限判断、状态机或场景结果。
- 不新增、删除或重写业务模块与业务字段。
- 不增加任何外部依赖、网络请求、Demo 跳转或本地服务依赖。

## 修改后对比结果

- 章节：12/12保留；顺序与标题不变。
- 顶部章节索引：12/12逐项点击并验证目标章节可见。
- 总览：12/12保留；从总览进入第01章验证非空白。
- 产品模块：12/12保留；字段、按钮、选项、状态、规则、SLA与权限结果不变。
- 场景：5/5离线运行；刷新恢复固定初始状态。
- 数据表：8/8保留；Asset仍为9条模拟设备。
- 用户授权的呈现变化：删除`OFFLINE`、新增页06流程—页面联动使用说明、替换两张流程图。
- 自动化结果：81/81；无外部请求、无控制台错误。
- 最新交互修订：章节切换由顶部索引、总览和底部上一页/下一页显式触发；第06章和第09章内部内容使用独立滚动，不再与章节导航竞争。
